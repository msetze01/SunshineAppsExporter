<UserControl x:Class="SunshineAppsExporter.SunshineAppsExporterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="Sunshine Installation Folder:"/>
        <TextBox Text="{Binding Settings.SunshineAppPath}"/>
        <TextBlock Text="Sunshine Assets Path:"/>
        <TextBox Text="{Binding Settings.SunshineAssetsPath}"/>
        <TextBlock Text="Sunshine Service Uri:"/>
        <TextBox Text="{Binding Settings.SunshineServiceUri}"/>
        <TextBlock Text="Sunshine Service Username:"/>
        <TextBox Text="{Binding Settings.SunshineUserName}"/>
        <TextBlock Text="Sunshine Service Password:"/>
        <TextBox Text="{Binding Settings.SunshinePassword}"/>
        <TextBlock Text="Export on library update?"/>
        <CheckBox IsChecked="{Binding Settings.ExportOnLibraryUpdate}" />
        <TextBlock Text="Export on game install?"/>
        <CheckBox IsChecked="{Binding Settings.ExportOnGameInstalled}" />
    </StackPanel>
</UserControl>